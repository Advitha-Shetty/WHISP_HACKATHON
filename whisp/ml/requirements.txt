# Versions below include pre-built wheels for Python 3.11–3.13 on Windows (no Rust needed).
# Older pins (e.g. pydantic 2.6.x) tried to compile pydantic-core on cp313 and failed without Cargo.

fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.35.0
pydantic>=2.10.0,<3.0.0
python-multipart>=0.0.9
httpx>=0.27.0,<0.29.0
